## Usage
in `dotfiles/` run `stow <dir_name>` on each to setup

## TODO
1. modify nvim to use vscode neovim
    (add `cond = not vim.g.vscode` to each file being disabled)
    namely, flash and mini can be keep, while other disabled
2. use shell script or makefile to setup

